--- a/webview/assets/error-boundary-BOla93vo.js
+++ b/webview/assets/error-boundary-BOla93vo.js
@@ -7589,6 +7589,7 @@
   Of = !0;
   let e = window.electronBridge?.getSentryInitOptions?.();
   if (!e) return;
+  if (e.enabled === !1) return;
   let t = u(e.buildFlavor),
     n = f(e.appVersion),
     r = e.buildFlavor !== `prod`;
