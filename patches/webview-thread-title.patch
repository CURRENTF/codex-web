--- a/webview/assets/local-conversation-thread-tEA3qvi1.js
+++ b/webview/assets/local-conversation-thread-tEA3qvi1.js
@@ -13459,6 +13459,7 @@
     k = a(O, e),
     A = a(N, e) === `projectless`,
     M = a(le, e),
     P = a(he, e),
+    codexHostedWindowTitle = a(Rn, e),
     {
       hasInheritedParentTurns: F,
       hasRenderableTurns: R,
@@ -13482,6 +13483,10 @@
   (0, Q.useEffect)(() => {
     J?.id == null || J.readAt != null || q(J.id);
   }, [J?.id, J?.readAt, q]);
+  (0, Q.useEffect)(() => {
+    let t = codexHostedWindowTitle?.trim();
+    t && (document.title = `${t} | Codex`);
+  }, [codexHostedWindowTitle]);
   let ue = $e(),
     { agentMode: de } = Tn({ conversationId: e, hostId: w }),
     fe = S ? ze(S) : null,
