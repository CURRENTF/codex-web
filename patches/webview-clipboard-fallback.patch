--- a/webview/assets/copy-to-clipboard-C9EKP1fU.js
+++ b/webview/assets/copy-to-clipboard-C9EKP1fU.js
@@ -1,43 +1,87 @@
+function n(e, t) {
+  let n = t ?? document,
+    r = n.createElement(`textarea`),
+    i = n.activeElement,
+    o = n.getSelection?.(),
+    l = [];
+  if (o) for (let e = 0; e < o.rangeCount; e += 1) l.push(o.getRangeAt(e));
+  ((r.value = e),
+    r.setAttribute(`readonly`, ``),
+    (r.style.position = `fixed`),
+    (r.style.top = `-1000px`),
+    (r.style.left = `-1000px`),
+    (r.style.opacity = `0`),
+    n.body.appendChild(r),
+    r.focus(),
+    r.select());
+  try {
+    return n.execCommand(`copy`);
+  } catch {
+    return !1;
+  } finally {
+    if ((n.body.removeChild(r), o)) {
+      o.removeAllRanges();
+      for (let e of l) o.addRange(e);
+    }
+    i?.focus?.();
+  }
+}
+function i(e, t) {
+  let n = t?.clipboardData;
+  if (n == null) return !1;
+  if (typeof e == `string`) n.setData(`text/plain`, e);
+  else
+    for (let [e, t] of Object.entries(e))
+      typeof t == `string` && n.setData(e, t);
+  t.preventDefault?.();
+  return !0;
+}
 function e(e, t) {
-  let { navigator: n } = t?.target?.ownerDocument?.defaultView ?? window;
-  return new Promise((t, r) => {
-    if (!n?.clipboard) {
-      r(Error(`Clipboard API unavailable`));
+  let r = t?.target?.ownerDocument?.defaultView ?? window,
+    { navigator: a, document: o } = r,
+    l = typeof e == `string` ? e : (e[`text/plain`] ?? ``);
+  return new Promise((s, c) => {
+    if (i(e, t)) {
+      s(!0);
       return;
     }
+    let d = () => {
+      n(l, o) ? s(!0) : c(Error(`Failed to copy to clipboard`));
+    };
+    if (!r.isSecureContext || !a?.clipboard) {
+      d();
+      return;
+    }
     try {
       if (
         typeof e != `string` &&
-        `write` in n.clipboard &&
+        `write` in a.clipboard &&
         typeof ClipboardItem < `u` &&
         `supports` in ClipboardItem
       ) {
-        let i = new ClipboardItem(
+        let n = new ClipboardItem(
           Object.fromEntries(
             Object.entries(e).map(([e, t]) => [
               e,
               typeof t == `string` ? new Blob([t], { type: e }) : t,
@@ -20,22 +64,9 @@
             ]),
           ),
         );
-        n.clipboard.write([i]).then(
-          () => t(!0),
-          () => {
-            r(Error(`Failed to copy to clipboard`));
-          },
-        );
+        a.clipboard.write([n]).then(() => s(!0), d);
       } else {
-        let i = typeof e == `string` ? e : (e[`text/plain`] ?? ``);
-        n.clipboard.writeText(i).then(
-          () => t(!0),
-          () => {
-            r(Error(`Failed to copy to clipboard`));
-          },
-        );
+        a.clipboard.writeText(l).then(() => s(!0), d);
       }
     } catch {
-      r(Error(`Failed to copy to clipboard`));
+      d();
     }
   });
 }
